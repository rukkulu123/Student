# Point this at your FastAPI backend when it's ready.
VITE_API_BASE_URL=http://localhost:8000/api

# "true" (default) uses the in-memory mock backend in src/mocks — the whole
# UI works without a real server. Set to "false" to call VITE_API_BASE_URL.
VITE_MOCK_MODE=true
